<nav class="bg-gray-800 p-4 text-white flex justify-between items-center">
  <div>
    <%= link_to 'Home', root_path, class: "font-bold text-lg hover:text-gray-300" %>
  </div>

  <div class="space-x-4">
    <% if user_signed_in? %>
        <%= link_to 'Categorias' , categories_path, class: "hover:text-gray-300" %>
        <%= link_to 'Requisitos do Sistema', system_requirements_path, class: "hover:text-gray-300" %>
        <div class="flex items-center space-x-4">
          <span class="text-gray-200 font-medium">Olá, <%= current_user.email %></span>
          <%= button_to 'Sair', destroy_user_session_path, method: :delete, class: "bg-red-600 hover:bg-red-700 text-white px-3 py-1 rounded focus:outline-none focus:ring-2 focus:ring-red-500" %>
        </div>
    <% else %>
      <%= link_to 'Entrar', new_user_session_path, class: "hover:text-gray-300" %>
      <%= link_to 'Registrar', new_user_registration_path, class: "hover:text-gray-300" %>
    <% end %>
  </div>
</nav>